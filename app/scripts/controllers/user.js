'use strict';

/**
 * @ngdoc function
 * @name hcrcApp.controller:UserCtrl
 * @description
 * # UserCtrl
 * Controller of the hcrcApp
 */
angular.module('hcrcApp')
  .controller('UserCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
